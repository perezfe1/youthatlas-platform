export const PAGINATION = {
  DEFAULT_PAGE_SIZE: 20,
  MAX_PAGE_SIZE: 100,
} as const;

export const DEADLINE_WARNING_DAYS = 7;

export const SEARCH = {
  MIN_QUERY_LENGTH: 2,
  DEBOUNCE_MS: 300,
} as const;
